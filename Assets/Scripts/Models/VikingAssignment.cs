namespace IdleViking.Models
{
    /// <summary>
    /// Where a viking is currently assigned.
    /// </summary>
    public enum VikingAssignment
    {
        Idle,
        Building,
        Party
    }
}
